<Window
    x:Class="Talah.Harness.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Talah.Harness.App.Models"
    mc:Ignorable="d"
    Title="Talah Harness">

  <Grid x:Name="RootGrid" Background="{StaticResource CanvasBrush}" Loaded="RootGrid_Loaded">
    <Grid.RowDefinitions>
      <RowDefinition Height="52" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <VisualStateManager.VisualStateGroups>
      <VisualStateGroup x:Name="AdaptiveLayoutStates">
        <VisualState x:Name="NarrowLayout">
          <VisualState.Setters>
            <Setter Target="TitleIdentityColumn.Width" Value="190" />
            <Setter Target="KernelRunway.Visibility" Value="Collapsed" />
            <Setter Target="SessionColumn.Width" Value="0" />
            <Setter Target="SessionSeparatorColumn.Width" Value="0" />
            <Setter Target="InspectorSeparatorColumn.Width" Value="0" />
            <Setter Target="InspectorColumn.Width" Value="0" />
            <Setter Target="SessionPane.Visibility" Value="Collapsed" />
            <Setter Target="SessionSeparator.Visibility" Value="Collapsed" />
            <Setter Target="InspectorSeparator.Visibility" Value="Collapsed" />
            <Setter Target="InspectorPane.Visibility" Value="Collapsed" />
            <Setter Target="OnboardingArtColumn.Width" Value="0" />
            <Setter Target="OnboardingContentColumn.Width" Value="*" />
            <Setter Target="OnboardingImage.Visibility" Value="Collapsed" />
            <Setter Target="OnboardingCard.Margin" Value="12" />
            <Setter Target="OnboardingContent.Padding" Value="20" />
            <Setter Target="OnboardingActions.Orientation" Value="Vertical" />
            <Setter Target="OnboardingActions.HorizontalAlignment" Value="Stretch" />
          </VisualState.Setters>
        </VisualState>
        <VisualState x:Name="MediumLayout">
          <VisualState.StateTriggers><AdaptiveTrigger MinWindowWidth="760" /></VisualState.StateTriggers>
          <VisualState.Setters>
            <Setter Target="TitleIdentityColumn.Width" Value="240" />
            <Setter Target="KernelRunway.Visibility" Value="Collapsed" />
            <Setter Target="SessionColumn.Width" Value="240" />
            <Setter Target="SessionSeparatorColumn.Width" Value="1" />
            <Setter Target="InspectorSeparatorColumn.Width" Value="0" />
            <Setter Target="InspectorColumn.Width" Value="0" />
            <Setter Target="SessionPane.Visibility" Value="Visible" />
            <Setter Target="SessionSeparator.Visibility" Value="Visible" />
            <Setter Target="InspectorSeparator.Visibility" Value="Collapsed" />
            <Setter Target="InspectorPane.Visibility" Value="Collapsed" />
            <Setter Target="OnboardingArtColumn.Width" Value="0" />
            <Setter Target="OnboardingContentColumn.Width" Value="*" />
            <Setter Target="OnboardingImage.Visibility" Value="Collapsed" />
            <Setter Target="OnboardingCard.Margin" Value="24" />
            <Setter Target="OnboardingContent.Padding" Value="30" />
            <Setter Target="OnboardingActions.Orientation" Value="Horizontal" />
            <Setter Target="OnboardingActions.HorizontalAlignment" Value="Right" />
          </VisualState.Setters>
        </VisualState>
        <VisualState x:Name="WideLayout">
          <VisualState.StateTriggers><AdaptiveTrigger MinWindowWidth="1180" /></VisualState.StateTriggers>
          <VisualState.Setters>
            <Setter Target="TitleIdentityColumn.Width" Value="296" />
            <Setter Target="KernelRunway.Visibility" Value="Visible" />
            <Setter Target="SessionColumn.Width" Value="296" />
            <Setter Target="SessionSeparatorColumn.Width" Value="1" />
            <Setter Target="InspectorSeparatorColumn.Width" Value="1" />
            <Setter Target="InspectorColumn.Width" Value="344" />
            <Setter Target="SessionPane.Visibility" Value="Visible" />
            <Setter Target="SessionSeparator.Visibility" Value="Visible" />
            <Setter Target="InspectorSeparator.Visibility" Value="Visible" />
            <Setter Target="InspectorPane.Visibility" Value="Visible" />
            <Setter Target="OnboardingArtColumn.Width" Value="1.15*" />
            <Setter Target="OnboardingContentColumn.Width" Value="0.85*" />
            <Setter Target="OnboardingImage.Visibility" Value="Visible" />
            <Setter Target="OnboardingCard.Margin" Value="36" />
            <Setter Target="OnboardingContent.Padding" Value="38,34" />
            <Setter Target="OnboardingActions.Orientation" Value="Horizontal" />
            <Setter Target="OnboardingActions.HorizontalAlignment" Value="Right" />
          </VisualState.Setters>
        </VisualState>
      </VisualStateGroup>
    </VisualStateManager.VisualStateGroups>

    <!-- Extended title bar / kernel runway -->
    <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource SubtleBorderBrush}" BorderThickness="0,0,0,1">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition x:Name="TitleIdentityColumn" Width="296" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Grid x:Name="TitleBarDragRegion" Grid.ColumnSpan="3" Background="Transparent" />

        <StackPanel Grid.Column="0" Margin="16,0,0,0" Orientation="Horizontal" VerticalAlignment="Center" Spacing="11">
          <Image Width="28" Height="28" Source="Assets/app-256.png" Stretch="Uniform" />
          <StackPanel VerticalAlignment="Center" Spacing="-2">
            <TextBlock FontFamily="Bahnschrift SemiCondensed" FontSize="17" FontWeight="SemiBold" Text="TALAH HARNESS" Foreground="{StaticResource PrimaryTextBrush}" />
            <TextBlock Style="{StaticResource InstrumentLabelStyle}" FontSize="9" Text="TRACE LEDGER / 1.0" />
          </StackPanel>
        </StackPanel>

        <ItemsRepeater x:Name="KernelRunway" Grid.Column="1" Margin="12,0" ItemsSource="{x:Bind Kernels}">
          <ItemsRepeater.Layout>
            <StackLayout Orientation="Horizontal" Spacing="8" />
          </ItemsRepeater.Layout>
          <ItemsRepeater.ItemTemplate>
            <DataTemplate x:DataType="models:KernelDisplayState">
              <Button MinHeight="44" Padding="10,6" CornerRadius="7" Background="Transparent" BorderBrush="{StaticResource SubtleBorderBrush}" BorderThickness="1" Click="KernelStatus_Click" AutomationProperties.Name="{x:Bind Name}" AutomationProperties.HelpText="Open kernel setup and health details">
                <StackPanel Orientation="Horizontal" Spacing="7">
                  <Ellipse Width="7" Height="7" Fill="{x:Bind IndicatorBrush}" VerticalAlignment="Center" />
                  <TextBlock FontFamily="Cascadia Mono" FontSize="11" Text="{x:Bind Name}" Foreground="{StaticResource PrimaryTextBrush}" />
                  <TextBlock FontSize="11" Text="{x:Bind Status, Mode=OneWay}" Foreground="{StaticResource SecondaryTextBrush}" />
                </StackPanel>
              </Button>
            </DataTemplate>
          </ItemsRepeater.ItemTemplate>
        </ItemsRepeater>

        <StackPanel Grid.Column="2" Margin="0,0,150,0" Orientation="Horizontal" VerticalAlignment="Center" Spacing="2">
          <Button Style="{StaticResource QuietIconButtonStyle}" Click="OpenDiagnostics_Click" ToolTipService.ToolTip="Diagnostics" AutomationProperties.Name="Open diagnostics">
            <FontIcon Glyph="&#xE9D9;" FontSize="16" />
          </Button>
          <Button Style="{StaticResource QuietIconButtonStyle}" Click="OpenSettings_Click" ToolTipService.ToolTip="Settings" AutomationProperties.Name="Open settings">
            <FontIcon Glyph="&#xE713;" FontSize="16" />
          </Button>
        </StackPanel>
      </Grid>
    </Border>

    <!-- Main three-pane workbench -->
    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition x:Name="SessionColumn" Width="296" />
        <ColumnDefinition x:Name="SessionSeparatorColumn" Width="1" />
        <ColumnDefinition Width="*" MinWidth="320" />
        <ColumnDefinition x:Name="InspectorSeparatorColumn" Width="1" />
        <ColumnDefinition x:Name="InspectorColumn" Width="344" />
      </Grid.ColumnDefinitions>

      <!-- Session ledger -->
      <Border x:Name="SessionPane" Grid.Column="0" Background="{StaticResource SurfaceBrush}">
        <Grid Padding="14,14,12,12">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
          </Grid.RowDefinitions>

          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Spacing="2">
              <TextBlock Style="{StaticResource InstrumentLabelStyle}" Text="SESSION LEDGER" />
              <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Workspace runs" />
            </StackPanel>
            <Button Grid.Column="1" Width="44" Height="44" CornerRadius="8" Click="NewSession_Click" AutomationProperties.Name="Create a new session">
              <FontIcon Glyph="&#xE710;" FontSize="16" />
            </Button>
          </Grid>

          <AutoSuggestBox Grid.Row="1" Margin="0,14,0,10" PlaceholderText="Filter sessions" QueryIcon="Find" TextChanged="SessionFilter_TextChanged" AutomationProperties.Name="Filter sessions" />

          <ListView x:Name="SessionList" Grid.Row="2" ItemsSource="{x:Bind Sessions}" SelectionChanged="SessionList_SelectionChanged" SelectionMode="Single" IsItemClickEnabled="False" AutomationProperties.Name="Sessions across running kernels" TabIndex="2">
            <ListView.ItemContainerStyle>
              <Style TargetType="ListViewItem">
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="Margin" Value="0,0,0,5" />
                <Setter Property="CornerRadius" Value="8" />
              </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
              <DataTemplate x:DataType="models:SessionDisplayState">
                <Grid Padding="10,9">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3" />
                    <ColumnDefinition Width="*" />
                  </Grid.ColumnDefinitions>
                  <Border Width="3" CornerRadius="2" Background="{x:Bind AccentBrush}" />
                  <StackPanel Grid.Column="1" Margin="10,0,0,0" Spacing="3">
                    <Grid>
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                      </Grid.ColumnDefinitions>
                      <TextBlock Text="{x:Bind Title}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" Foreground="{StaticResource PrimaryTextBrush}" />
                      <TextBlock Grid.Column="1" Text="{x:Bind UpdatedLabel}" FontFamily="Cascadia Mono" FontSize="10" Foreground="{StaticResource SecondaryTextBrush}" />
                    </Grid>
                    <TextBlock Text="{x:Bind Preview}" MaxLines="2" TextTrimming="CharacterEllipsis" FontSize="12" Foreground="{StaticResource SecondaryTextBrush}" />
                    <TextBlock Text="{x:Bind KernelName}" FontFamily="Cascadia Mono" FontSize="9" CharacterSpacing="60" Foreground="{x:Bind AccentBrush}" />
                  </StackPanel>
                </Grid>
              </DataTemplate>
            </ListView.ItemTemplate>
          </ListView>

          <Button Grid.Row="3" MinHeight="44" Margin="0,10,0,0" HorizontalAlignment="Stretch" HorizontalContentAlignment="Left" Background="Transparent" BorderBrush="{StaticResource SubtleBorderBrush}" BorderThickness="1" CornerRadius="8" Click="ChooseWorkspace_Click" AutomationProperties.Name="Choose workspace" AutomationProperties.HelpText="Choose the approved root for sessions and attachments">
            <StackPanel Orientation="Horizontal" Spacing="10">
              <FontIcon Glyph="&#xE838;" FontSize="15" />
              <StackPanel>
                <TextBlock Text="Workspace" FontWeight="SemiBold" />
                <TextBlock x:Name="WorkspacePathText" Text="Choose a folder" FontSize="11" Foreground="{StaticResource SecondaryTextBrush}" TextTrimming="CharacterEllipsis" MaxWidth="210" />
              </StackPanel>
            </StackPanel>
          </Button>
        </Grid>
      </Border>

      <Border x:Name="SessionSeparator" Grid.Column="1" Background="{StaticResource SubtleBorderBrush}" />

      <!-- Trace ledger -->
      <Grid Grid.Column="2" Background="{StaticResource CanvasBrush}">
        <Grid.RowDefinitions>
          <RowDefinition Height="64" />
          <RowDefinition Height="*" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="22,0" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource SubtleBorderBrush}" BorderThickness="0,0,0,1">
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel VerticalAlignment="Center" Spacing="1">
              <TextBlock x:Name="ConversationTitleText" Style="{StaticResource SectionTitleStyle}" FontSize="20" Text="No active session" />
              <TextBlock x:Name="ConversationMetaText" Style="{StaticResource InstrumentLabelStyle}" Text="SELECT A KERNEL AND WORKSPACE TO BEGIN" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Spacing="2">
              <Button x:Name="RefreshSessionButton" Style="{StaticResource QuietIconButtonStyle}" Click="RefreshSession_Click" ToolTipService.ToolTip="Refresh session" AutomationProperties.Name="Refresh session">
                <FontIcon Glyph="&#xE72C;" FontSize="16" />
              </Button>
              <Button x:Name="SessionMenuButton" Style="{StaticResource QuietIconButtonStyle}" Click="OpenSessionMenu_Click" ToolTipService.ToolTip="Session actions" AutomationProperties.Name="Session actions">
                <FontIcon Glyph="&#xE712;" FontSize="16" />
              </Button>
            </StackPanel>
          </Grid>
        </Border>

        <Grid Grid.Row="1">
          <ScrollViewer x:Name="TraceScrollViewer" Padding="24,18,28,18">
            <ItemsRepeater ItemsSource="{x:Bind TraceItems}">
              <ItemsRepeater.Layout>
                <StackLayout Spacing="0" />
              </ItemsRepeater.Layout>
              <ItemsRepeater.ItemTemplate>
                <DataTemplate x:DataType="models:TraceDisplayState">
                  <Grid MinHeight="76">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="38" />
                      <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Border Width="2" HorizontalAlignment="Center" Background="{StaticResource TraceRailBrush}" />
                    <Grid Width="26" Height="26" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,10,0,0">
                      <Ellipse Fill="{StaticResource SurfaceBrush}" Stroke="{x:Bind AccentBrush}" StrokeThickness="2" />
                      <FontIcon Glyph="{x:Bind Glyph}" Foreground="{x:Bind AccentBrush}" FontSize="12" />
                    </Grid>
                    <Border Grid.Column="1" Margin="8,4,0,14" Padding="14,11" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource SubtleBorderBrush}" BorderThickness="1" CornerRadius="8">
                      <StackPanel Spacing="6">
                        <Grid>
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                          </Grid.ColumnDefinitions>
                          <TextBlock Text="{x:Bind KindLabel}" FontFamily="Cascadia Mono" FontSize="10" CharacterSpacing="70" Foreground="{x:Bind AccentBrush}" />
                          <TextBlock Grid.Column="1" Margin="10,0,0,0" Text="{x:Bind Title}" FontWeight="SemiBold" Foreground="{StaticResource PrimaryTextBrush}" />
                          <TextBlock Grid.Column="2" Text="{x:Bind Timestamp}" FontFamily="Cascadia Mono" FontSize="10" Foreground="{StaticResource SecondaryTextBrush}" />
                        </Grid>
                        <TextBlock Text="{x:Bind Body}" TextWrapping="Wrap" IsTextSelectionEnabled="True" Foreground="{StaticResource PrimaryTextBrush}" />
                      </StackPanel>
                    </Border>
                  </Grid>
                </DataTemplate>
              </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
          </ScrollViewer>

          <Grid x:Name="EmptySessionPanel" Padding="52,34" Background="{StaticResource CanvasBrush}">
            <Grid.RowDefinitions>
              <RowDefinition Height="*" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Image MaxWidth="620" MaxHeight="430" HorizontalAlignment="Center" VerticalAlignment="Center" Source="Assets/Brand/empty-session-ledger.png" Stretch="Uniform" Opacity="0.92" />
            <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,16,0,20" Spacing="8">
              <TextBlock HorizontalAlignment="Center" FontFamily="Bahnschrift SemiCondensed" FontSize="28" FontWeight="SemiBold" Text="The ledger is ready" Foreground="{StaticResource PrimaryTextBrush}" />
              <TextBlock MaxWidth="520" TextAlignment="Center" TextWrapping="Wrap" Text="Choose a workspace and a kernel. Every prompt, approval, command, file change, and result will remain on one inspectable trace." Foreground="{StaticResource SecondaryTextBrush}" />
              <Button Margin="0,8,0,0" HorizontalAlignment="Center" Click="NewSession_Click" Content="Start a session" />
            </StackPanel>
          </Grid>
        </Grid>

        <Border Grid.Row="2" Margin="18,8,18,16" Padding="12" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource SubtleBorderBrush}" BorderThickness="1" CornerRadius="10">
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBox x:Name="PromptTextBox" MinHeight="68" MaxHeight="220" AcceptsReturn="True" TextWrapping="Wrap" PlaceholderText="Describe the change, investigation, or command…" BorderThickness="0" Background="Transparent" IsEnabled="False" KeyDown="PromptTextBox_KeyDown" AutomationProperties.Name="Agent prompt" AutomationProperties.HelpText="Control Enter sends or steers the active kernel" TabIndex="3" />
            <ItemsControl x:Name="AttachmentList" Grid.Row="1" Margin="2,4,0,2" ItemsSource="{x:Bind Attachments}">
              <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate><ItemsWrapGrid Orientation="Horizontal" /></ItemsPanelTemplate>
              </ItemsControl.ItemsPanel>
              <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="models:AttachmentDisplayState">
                  <Border Margin="0,0,6,4" Padding="8,4" Background="{StaticResource SurfaceRaisedBrush}" BorderBrush="{StaticResource SubtleBorderBrush}" BorderThickness="1" CornerRadius="6">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                      <TextBlock Text="{x:Bind Name}" VerticalAlignment="Center" FontFamily="Cascadia Mono" FontSize="10" />
                      <Button Padding="4" Background="Transparent" Click="RemoveAttachment_Click" AutomationProperties.Name="Remove attachment"><FontIcon Glyph="&#xE711;" FontSize="10" /></Button>
                    </StackPanel>
                  </Border>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Grid Grid.Row="2" Margin="2,8,0,0">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <StackPanel Orientation="Horizontal" Spacing="2">
                <Button x:Name="AttachButton" Style="{StaticResource QuietIconButtonStyle}" Click="AttachFiles_Click" IsEnabled="False" ToolTipService.ToolTip="Attach files" AutomationProperties.Name="Attach files" AutomationProperties.HelpText="Only files inside the approved workspace may be attached">
                  <FontIcon Glyph="&#xE723;" FontSize="16" />
                </Button>
                <Button Style="{StaticResource QuietIconButtonStyle}" Click="ChooseWorkspace_Click" ToolTipService.ToolTip="Change workspace" AutomationProperties.Name="Change workspace">
                  <FontIcon Glyph="&#xE838;" FontSize="16" />
                </Button>
                <TextBlock x:Name="ComposerStatusText" Margin="8,0,0,0" VerticalAlignment="Center" FontFamily="Cascadia Mono" FontSize="10" Text="NO ACTIVE KERNEL" Foreground="{StaticResource SecondaryTextBrush}" AutomationProperties.LiveSetting="Polite" />
              </StackPanel>
              <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6">
                <Button x:Name="StopButton" MinHeight="44" Visibility="Collapsed" Click="StopTurn_Click" Content="Stop" AutomationProperties.Name="Cancel active turn" />
                <Button x:Name="SendButton" MinHeight="44" IsEnabled="False" Click="SendPrompt_Click" Content="Run" AutomationProperties.Name="Run prompt" TabIndex="4" />
              </StackPanel>
            </Grid>
          </Grid>
        </Border>
      </Grid>

      <Border x:Name="InspectorSeparator" Grid.Column="3" Background="{StaticResource SubtleBorderBrush}" />

      <!-- Inspector -->
      <Border x:Name="InspectorPane" Grid.Column="4" Background="{StaticResource SurfaceBrush}">
        <ScrollViewer Padding="16,14,14,18">
          <StackPanel Spacing="16">
            <StackPanel Spacing="2">
              <TextBlock Style="{StaticResource InstrumentLabelStyle}" Text="INSPECTOR" />
              <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Run context" />
            </StackPanel>

            <Border Padding="12" Background="{StaticResource SurfaceRaisedBrush}" BorderBrush="{StaticResource SubtleBorderBrush}" BorderThickness="1" CornerRadius="8">
              <StackPanel Spacing="10">
                <TextBlock Text="Kernel" FontWeight="SemiBold" />
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                  </Grid.ColumnDefinitions>
                  <TextBlock x:Name="InspectorKernelName" Text="None selected" Foreground="{StaticResource SecondaryTextBrush}" />
                  <Border Grid.Column="1" Padding="7,3" CornerRadius="10" BorderBrush="{StaticResource SubtleBorderBrush}" BorderThickness="1">
                    <TextBlock x:Name="InspectorKernelState" FontFamily="Cascadia Mono" FontSize="9" Text="OFFLINE" />
                  </Border>
                </Grid>
              </StackPanel>
            </Border>

            <StackPanel Spacing="5">
              <TextBlock Text="Session and profile" FontWeight="SemiBold" />
              <TextBlock x:Name="InspectorSessionText" Text="No session selected" TextWrapping="Wrap" Foreground="{StaticResource SecondaryTextBrush}" />
              <TextBlock x:Name="InspectorWorkspaceText" Text="No workspace selected" TextWrapping="Wrap" FontFamily="Cascadia Mono" FontSize="10" Foreground="{StaticResource SecondaryTextBrush}" />
            </StackPanel>

            <StackPanel Spacing="8">
              <TextBlock Text="Security boundary" FontWeight="SemiBold" />
              <InfoBar x:Name="SecurityInfoBar" IsOpen="True" IsClosable="False" Severity="Informational" Title="No kernel selected" Message="The selected kernel's actual enforcement owner, writable roots, process limits, and network policy appear here." />
            </StackPanel>

            <StackPanel Spacing="8">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Workspace impact" FontWeight="SemiBold" />
                <Button x:Name="RefreshDiffButton" Grid.Column="1" Style="{StaticResource QuietIconButtonStyle}" IsEnabled="False" Click="RefreshDiff_Click" AutomationProperties.Name="Refresh workspace impact">
                  <FontIcon Glyph="&#xE72C;" FontSize="13" />
                </Button>
              </Grid>
              <Border Padding="13,12" BorderBrush="{StaticResource SubtleBorderBrush}" BorderThickness="1" CornerRadius="8">
                <TextBlock x:Name="DiffText" MaxHeight="260" Text="No file changes reported by the selected kernel." TextWrapping="NoWrap" IsTextSelectionEnabled="True" FontFamily="Cascadia Mono" FontSize="10" Foreground="{StaticResource SecondaryTextBrush}" />
              </Border>
            </StackPanel>

            <StackPanel Spacing="5">
              <TextBlock Text="Usage" FontWeight="SemiBold" />
              <TextBlock x:Name="UsageText" Text="No token or cost totals reported." TextWrapping="Wrap" FontFamily="Cascadia Mono" FontSize="10" Foreground="{StaticResource SecondaryTextBrush}" AutomationProperties.LiveSetting="Polite" />
            </StackPanel>

            <StackPanel Spacing="8">
              <TextBlock Text="Capabilities" FontWeight="SemiBold" />
              <ItemsControl x:Name="CapabilityList">
                <ItemsControl.ItemTemplate>
                  <DataTemplate>
                    <Grid Margin="0,3">
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                      </Grid.ColumnDefinitions>
                      <FontIcon Glyph="&#xE73E;" FontSize="11" Foreground="{StaticResource VerifiedGreenBrush}" />
                      <TextBlock Grid.Column="1" Margin="8,0,0,0" Text="{Binding}" FontSize="12" />
                    </Grid>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
            </StackPanel>

            <Expander Header="Diagnostics" IsExpanded="False">
              <TextBlock x:Name="DiagnosticsText" Margin="0,8,0,0" FontFamily="Cascadia Mono" FontSize="10" TextWrapping="Wrap" IsTextSelectionEnabled="True" Text="Runtime discovery has not completed." Foreground="{StaticResource SecondaryTextBrush}" />
            </Expander>
          </StackPanel>
        </ScrollViewer>
      </Border>
    </Grid>

    <InfoBar x:Name="TransientInfoBar" Grid.Row="1" Margin="16" HorizontalAlignment="Center" VerticalAlignment="Top" MaxWidth="680" IsOpen="False" IsClosable="True" />

    <!-- First-run overlay -->
    <Grid x:Name="OnboardingOverlay" Grid.RowSpan="2" Background="{StaticResource OverlayBrush}" Visibility="Collapsed">
      <Border x:Name="OnboardingCard" MaxWidth="1120" MaxHeight="720" Margin="36" HorizontalAlignment="Center" VerticalAlignment="Center" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource ChainCyanBrush}" BorderThickness="1" CornerRadius="14">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="OnboardingArtColumn" Width="1.15*" />
            <ColumnDefinition x:Name="OnboardingContentColumn" Width="0.85*" />
          </Grid.ColumnDefinitions>
          <Image x:Name="OnboardingImage" Grid.Column="0" Source="Assets/Brand/onboarding-kernel-traces.png" Stretch="UniformToFill" />
          <ScrollViewer Grid.Column="1" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
          <StackPanel x:Name="OnboardingContent" Padding="38,34" VerticalAlignment="Center" Spacing="17">
            <TextBlock Style="{StaticResource InstrumentLabelStyle}" Text="TALAH HARNESS / FIRST RUN" />
            <TextBlock FontFamily="Bahnschrift SemiCondensed" FontSize="42" FontWeight="SemiBold" Text="Three kernels. One accountable trace." TextWrapping="Wrap" Foreground="{StaticResource PrimaryTextBrush}" />
            <TextBlock FontSize="15" LineHeight="23" Text="Choose Codex, OpenCode, or TLAH for each session. The Harness keeps the workspace, approvals, process lifecycle, and durable audit trail visible without pretending the engines have identical security guarantees." TextWrapping="Wrap" Foreground="{StaticResource SecondaryTextBrush}" />
            <StackPanel Spacing="10">
              <StackPanel Orientation="Horizontal" Spacing="10">
                <Ellipse Width="8" Height="8" Fill="{StaticResource ChainCyanBrush}" VerticalAlignment="Center" />
                <TextBlock Text="Official subscription login and API keys, isolated per profile" TextWrapping="Wrap" />
              </StackPanel>
              <StackPanel Orientation="Horizontal" Spacing="10">
                <Ellipse Width="8" Height="8" Fill="{StaticResource EvidenceGoldBrush}" VerticalAlignment="Center" />
                <TextBlock Text="Every permission remains attached to its native kernel meaning" TextWrapping="Wrap" />
              </StackPanel>
              <StackPanel Orientation="Horizontal" Spacing="10">
                <Ellipse Width="8" Height="8" Fill="{StaticResource VerifiedGreenBrush}" VerticalAlignment="Center" />
                <TextBlock Text="Interrupted sessions remain recoverable and inspectable" TextWrapping="Wrap" />
              </StackPanel>
            </StackPanel>
            <TextBlock x:Name="OnboardingHealthText" FontFamily="Cascadia Mono" FontSize="10" TextWrapping="Wrap" Foreground="{StaticResource SecondaryTextBrush}" AutomationProperties.LiveSetting="Polite" />
            <TextBlock x:Name="OnboardingWorkspaceText" Text="Workspace not selected" TextWrapping="Wrap" Foreground="{StaticResource SecondaryTextBrush}" />
            <StackPanel x:Name="OnboardingActions" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
              <Button Click="ReviewSecurity_Click" Content="Review security" />
              <Button Click="ChooseWorkspace_Click" Content="Choose workspace" />
              <Button Click="CompleteOnboarding_Click" Content="Finish setup" />
            </StackPanel>
          </StackPanel>
          </ScrollViewer>
        </Grid>
      </Border>
    </Grid>
  </Grid>
</Window>

